---
description: Initialize the Shopify Storeleads Database project
---
1. Install Node.js dependencies
// turbo
2. npm install

3. Check if Python is installed and install Python dependencies
// turbo
4. pip install psycopg2-binary

5. Check if .env file exists and guide user to configure it
6. echo "Please check .env file and configure your Neon database connection strings."

7. Run database setup instructions (User needs to do this manually or provide credentials)
